# UF0518 TDD RED checkpoint

Before implementation, the branch contains tests that require:

- source traceability to support multiple manifests;
- validators not to hard-code the published total as 450.

The production validators have not yet been modified. CI is expected to fail at this checkpoint for those reasons.
